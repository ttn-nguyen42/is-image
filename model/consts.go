package model

/*
 * API versions
 */
const (
	V1 = "/api/v1"
)

/*
 * Paths
 */
const (
	UploadImage = "/upload_image"
)

/*
 * Environment variables
 */
const (
	PORT      = "PORT"
	MONGO_URL = "MONGO_URL"
)
